==============================================================================
agy CLI print-mode captures (--print)
Captured 2026-06-07, agy version: 1.0.6:
==============================================================================

--- TEST 1: bare --print with positional prompt ---
$ agy --print "Reply with the word PONG only" --dangerously-skip-permissions
PONG
EXIT: 0

--- TEST 2: minimal math prompt ---
$ agy --print "What does 2+2 equal" --dangerously-skip-permissions
2 + 2 equals 4.
EXIT: 0

--- TEST 3: --print without positional (parse behavior probe) ---
$ agy --print --dangerously-skip-permissions  # no positional prompt
It looks like you've provided a flag, `--dangerously-skip-permissions`. Could you please provide more context on what you're trying to do? Are you trying to run a specific command or script with this flag, or is there a particular task you'd like me to help you with?
EXIT: 0

--- TEST 4: help output for reference ---
$ agy --help
Usage of agy:
  --add-dir                       Add a directory to the workspace (repeatable) (default [])
  -c                              Short alias for --continue
  --continue                      Continue the most recent conversation
  --conversation                  Resume a previous conversation by ID
  --dangerously-skip-permissions  Auto-approve all tool permission requests without prompting
  -i                              Short alias for --prompt-interactive
  --log-file                      Override CLI log file path
  --model                         Model for the current CLI session
  -p                              Short alias for --print
  --print                         Run a single prompt non-interactively and print the response
  --print-timeout                 Timeout for print mode wait (default 5m0s)
  --prompt                        Alias for --print
  --prompt-interactive            Run an initial prompt interactively and continue the session
  --sandbox                       Run in a sandbox with terminal restrictions enabled

Available subcommands:
  changelog       Show changelog and release notes
  help            Show help for subcommands
  install         Configure environment paths and shell settings
  models          List available models
  plugin          Manage plugins (install, uninstall, list, enable, disable)
  plugins         Alias for plugin
  update          Update CLI
